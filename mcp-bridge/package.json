{
  "name": "xyvirtual-webviewer-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local Node/TS MCP bridge: stdio MCP server <-> XYvirtual WEB browser over WebSocket (:18714).",
  "license": "AGPL-3.0-only",
  "main": "dist/index.js",
  "bin": {
    "xyvirtual-webviewer-mcp": "dist/index.js"
  },
  "scripts": {
    "setup": "npm install && npm run build",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
